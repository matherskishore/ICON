import { ReviewerIRFStatus } from './ReviewerIRFStatus';

export class ReviewerIRFHome {
  ToBeApproved: Array<ReviewerIRFStatus>;
  ApprovedToBeIntegrated: Array<ReviewerIRFStatus>;
  IntegratedAndTobeInvoiced: Array<ReviewerIRFStatus>;

}
